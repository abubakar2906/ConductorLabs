# Claude Code local tooling config — not app code, not needed by collaborators.
.claude/

# Internal engineering notes — a deliverable for the founder, not repo docs.
ENGINEERING_REPORT.md

# Helix sandbox command-overflow artifacts — local tooling, never ship them.
.helix-overflow/
